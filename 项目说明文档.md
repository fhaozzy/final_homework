# 实验室设备与耗材借用管理系统 - 项目说明文档

## 目录
- [1. 项目概述](#1-项目概述)
- [2. 角色与权限矩阵](#2-角色与权限矩阵)
- [3. 数据库设计](#3-数据库设计)
- [4. 状态机设计](#4-状态机设计)
- [5. 关键业务规则](#5-关键业务规则)
- [6. REST接口设计](#6-rest接口设计)
- [7. 前端页面与路由](#7-前端页面与路由)
- [8. 软件系统实现](#8-软件系统实现)
- [9. 数据库范式分析](#9-数据库范式分析)
- [10. 数据库引擎配置](#10-数据库引擎配置)

---

## 1. 项目概述

### 1.1 项目简介
本项目是一个完整的实验室设备与耗材借用管理系统，旨在实现实验室资源的数字化管理，提高设备借用效率和耗材库存管理水平。

### 1.2 核心功能模块
- **用户与角色管理**：多角色权限控制（STUDENT、TEACHER、MAINTAINER、ADMIN）
- **设备借用流程**：申请、审批、出借、归还验收全流程管理
- **耗材与库存管理**：库存监控、出入库管理、安全库存预警
- **设备维修管理**：维修申请、处理、记录
- **报表统计**：借用排行、设备利用率、耗材月消耗

### 1.3 技术架构
- **后端**：Django + DRF + SimpleJWT + MySQL
- **前端**：Vue3 + Vite + ElementPlus + Pinia + Axios
- **认证**：JWT Token认证（SimpleJWT）
- **数据库**：MySQL（InnoDB引擎，utf8mb4字符集）

---

## 2. 角色与权限矩阵

### 2.1 角色定义

| 角色 | 角色编码 | 主要职责 |
|------|----------|----------|
| 学生 | STUDENT | 可发起设备借用/耗材领用；仅可查看本人单据；不可审批/出入库/验收 |
| 老师 | TEACHER | 可发起设备借用/耗材领用；可查看全量借用单（只读）与统计报表（只读）；不可审批/出入库/验收 |
| 维修员 | MAINTAINER | 可创建/更新维修单（写入）；其余只读；若无则管理员兼任 |
| 管理员 | ADMIN | 唯一审批人；负责出库/归还验收/库存审核；可管理用户与角色；可查看所有数据 |

### 2.2 接口策略（以 `/api/v1/` 为准）

| 接口类型 | 路径 | 权限说明 |
|----------|------|----------|
| 无需登录 | `/health/`、`/auth/token/`、`/auth/token/refresh/` | 公开访问 |
| 当前用户信息 | `GET /api/v1/me/` | 返回用户资料 + `role.code` 列表 + `is_admin` |
| 用户与角色 | `/users/`、`/roles/`、`/user-roles/` | 仅管理员 |
| 设备台账 | `/equipment/`、`/equipment-categories/`、`/equipment/{id}/status/` | 读：所有登录用户；写：仅管理员 |
| 借用流程 | `/borrow/requests/` 及子接口 | 创建：学生/老师/管理员；列表/详情：管理员全量；老师全量（只读）；学生仅本人（只读）；审批/出库/归还验收：仅管理员 |
| 耗材与库存 | `/consumables/`、`/stock/`、`/stock/in/`、`/stock/out/` | 耗材台账读/预警：所有登录用户；耗材台账写：仅管理员；领用申请：学生/老师/管理员；入库/审核：仅管理员；流水查询：管理员全量；其余仅本人 |
| 维修 | `/maintenance/` | 读：所有登录用户；写：管理员/维修员 |
| 报表 | `/reports/borrow-top/`、`/reports/equipment-utilization/`、`/reports/consumable-monthly/` | 管理员/老师可读；学生无权限 |

### 2.3 角色权限矩阵

| 功能模块 | 学生 | 老师 | 维修员 | 管理员 |
|----------|------|------|--------|--------|
| 查看设备列表 | ✓ | ✓ | ✓ | ✓ |
| 提交借用申请 | ✓ | ✓ | - | ✓ |
| 查看借用申请列表（本人） | ✓ | - | - | - |
| 查看借用申请列表（全量） | - | ✓（只读） | - | ✓ |
| 审批借用申请 | - | - | - | ✓ |
| 设备出库 | - | - | - | ✓ |
| 设备归还验收 | - | - | - | ✓ |
| 查看耗材库存 | ✓ | ✓ | ✓ | ✓ |
| 提交耗材领用申请 | ✓ | ✓ | - | ✓ |
| 耗材入库 | - | - | - | ✓ |
| 耗材领用审核 | - | - | - | ✓ |
| 查看库存流水（本人） | ✓ | ✓ | - | - |
| 查看库存流水（全量） | - | - | - | ✓ |
| 创建维修记录 | - | - | ✓ | ✓ |
| 更新维修记录 | - | - | ✓ | ✓ |
| 查看维修列表 | ✓ | ✓ | ✓ | ✓ |
| 查看报表 | - | ✓ | - | ✓ |
| 用户管理 | - | - | - | ✓ |
| 角色管理 | - | - | - | ✓ |

---

## 3. 数据库设计

### 3.1 数据库表概览
系统共包含**13张业务数据表**，分为4个模块：

#### 3.1.1 用户与角色模块（3张表）
| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `user` | 用户表 | id, username(uniq), password(哈希), email, real_name, phone, is_active, is_staff, date_joined, created_at, updated_at |
| `role` | 角色表 | id, code(uniq), name, created_at, updated_at |
| `user_role` | 用户角色关联表 | id, user_id FK user, role_id FK role, created_at, updated_at；uniq(user_id,role_id)；idx(user_id), idx(role_id) |

#### 3.1.2 设备与库存模块（5张表）
| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `equipment_category` | 设备分类表 | id, name(uniq), code(uniq), description, created_at, updated_at |
| `equipment` | 设备表 | id, code(uniq), name, category_id FK equipment_category, status(enum), location, purchase_date, price, vendor, spec, usable, created_at, updated_at；idx(category_id), idx(status) |
| `equipment_status_log` | 设备状态日志表 | id, equipment_id FK equipment, from_status, to_status, changed_by FK user, reason, remark, created_at, updated_at；idx(equipment_id) |
| `consumable` | 耗材表 | id, code(uniq), name, unit, category, safety_stock, current_stock, location, created_at, updated_at；idx(code), idx(current_stock) |
| `stock_txn` | 库存交易表 | id, consumable_id FK consumable, type(enum IN/OUT), status(enum PENDING/APPROVED/REJECTED), qty(>0), related_request_id FK borrow_request(nullable), performed_by FK user, reviewed_by FK user(nullable), decided_at(nullable), remark, created_at, updated_at；idx(consumable_id,type,created_at), idx(status) |

#### 3.1.3 借用管理模块（4张表）
| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `borrow_request` | 借用申请表 | id, applicant_id FK user, type(enum EQUIPMENT/CONSUMABLE), status(enum REQUESTED/APPROVED/REJECTED/OUT/CLOSED), purpose, expected_return_date, submitted_at, approved_at, rejected_at, approver_id FK user(nullable, 固定管理员), remark, created_at, updated_at；idx(applicant_id,status,type) |
| `borrow_item` | 借用明细表 | id, request_id FK borrow_request, item_type(enum EQUIPMENT/CONSUMABLE), equipment_id FK equipment(nullable), consumable_id FK consumable(nullable), qty(>0), status(enum PENDING/OUT/RETURN_PENDING/RETURN_ACCEPTED/RETURN_REJECTED), out_at, due_at, return_submitted_at, return_checked_at, created_at, updated_at；uniq(request_id,equipment_id)；idx(request_id,status) |
| `borrow_approval` | 借用审批表 | id, request_id OneToOne borrow_request, approver_id FK user, decision(enum APPROVED/REJECTED), comment, decided_at, created_at, updated_at |
| `return_record` | 归还记录表 | id, borrow_item_id OneToOne borrow_item, checked_by FK user, condition(enum GOOD/DAMAGED/LOST), fee, remark, checked_at, created_at, updated_at |

#### 3.1.4 维修管理模块（1张表）
| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `maintenance` | 维修记录表 | id, equipment_id FK equipment, title, description, status(enum OPEN/IN_PROGRESS/DONE), assigned_to FK user(nullable), cost, started_at, finished_at, created_at, updated_at；idx(equipment_id), idx(status) |

### 3.2 表结构详细说明

#### 3.2.1 用户与角色模块

**user表**
```python
- id: 主键（bigint自增）
- username: 用户名（唯一，继承自AbstractUser）
- password: 密码（哈希加密）
- email: 邮箱
- real_name: 真实姓名
- phone: 联系电话
- is_active: 是否激活
- is_staff: 是否为员工（管理员标记）
- date_joined: 加入时间
- created_at: 创建时间
- updated_at: 更新时间
```

**role表**
```python
- id: 主键（bigint自增）
- code: 角色编码（唯一，如STUDENT、TEACHER、MAINTAINER、ADMIN）
- name: 角色名称
- created_at: 创建时间
- updated_at: 更新时间
```

**user_role表**
```python
- id: 主键（bigint自增）
- user_id: 用户ID（外键 → user.id，级联删除）
- role_id: 角色ID（外键 → role.id，级联删除）
- created_at: 创建时间
- updated_at: 更新时间
- 约束: 唯一约束 (user_id, role_id)
- 索引: user_id, role_id
```

#### 3.2.2 设备与库存模块

**equipment_category表**
```python
- id: 主键（bigint自增）
- name: 分类名称（唯一）
- code: 分类编码（唯一）
- description: 描述
- created_at: 创建时间
- updated_at: 更新时间
```

**equipment表**
```python
- id: 主键（bigint自增）
- code: 设备编码（唯一）
- name: 设备名称
- category_id: 分类ID（外键 → equipment_category.id，保护删除）
- status: 状态（AVAILABLE/RESERVED/BORROWED/OUT(legacy)/RETURN_PENDING/MAINTENANCE/DISCARDED）
- location: 存放位置
- purchase_date: 购买日期
- price: 价格
- vendor: 供应商
- spec: 规格
- usable: 是否可用
- created_at: 创建时间
- updated_at: 更新时间
- 索引: category_id, status
```

**equipment_status_log表**
```python
- id: 主键（bigint自增）
- equipment_id: 设备ID（外键 → equipment.id，级联删除）
- from_status: 原状态
- to_status: 新状态
- changed_by: 操作人ID（外键 → user.id，保护删除）
- reason: 原因
- remark: 备注
- created_at: 创建时间
- updated_at: 更新时间
- 索引: equipment_id
```

**consumable表**
```python
- id: 主键（bigint自增）
- code: 耗材编码（唯一）
- name: 耗材名称
- unit: 单位
- category: 分类
- safety_stock: 安全库存
- current_stock: 当前库存
- location: 存放位置
- created_at: 创建时间
- updated_at: 更新时间
- 索引: code, current_stock
```

**stock_txn表**
```python
- id: 主键（bigint自增）
- consumable_id: 耗材ID（外键 → consumable.id，级联删除）
- type: 交易类型（IN/OUT）
- status: 状态（PENDING/APPROVED/REJECTED）
- qty: 数量（>0）
- related_request_id: 关联申请ID（外键 → borrow_request.id，置空删除）
- performed_by: 操作人ID（外键 → user.id，保护删除）
- reviewed_by: 审核人ID（外键 → user.id，保护删除，可为空）
- decided_at: 决策时间
- remark: 备注
- created_at: 创建时间
- updated_at: 更新时间
- 索引: (consumable_id, type, created_at), status
```

#### 3.2.3 借用管理模块

**borrow_request表**
```python
- id: 主键（bigint自增）
- applicant_id: 申请人ID（外键 → user.id，保护删除）
- type: 申请类型（EQUIPMENT/CONSUMABLE）
- status: 状态（REQUESTED/APPROVED/REJECTED/OUT/CLOSED）
- purpose: 用途
- expected_return_date: 预期归还日期
- submitted_at: 提交时间
- approved_at: 审批通过时间
- rejected_at: 审批拒绝时间
- approver_id: 审批人ID（外键 → user.id，保护删除，可为空，固定管理员）
- remark: 备注
- created_at: 创建时间
- updated_at: 更新时间
- 索引: (applicant_id, status, type)
```

**borrow_item表**
```python
- id: 主键（bigint自增）
- request_id: 申请ID（外键 → borrow_request.id，级联删除）
- item_type: 物品类型（EQUIPMENT/CONSUMABLE）
- equipment_id: 设备ID（外键 → equipment.id，保护删除，可为空）
- consumable_id: 耗材ID（外键 → consumable.id，保护删除，可为空）
- qty: 数量（>0）
- status: 状态（PENDING/OUT/RETURN_PENDING/RETURN_ACCEPTED/RETURN_REJECTED）
- out_at: 出借时间
- due_at: 应归还时间
- return_submitted_at: 归还提交时间
- return_checked_at: 归还验收时间
- created_at: 创建时间
- updated_at: 更新时间
- 约束: 唯一约束 (request_id, equipment_id)
- 索引: (request_id, status)
```

**borrow_approval表**
```python
- id: 主键（bigint自增）
- request_id: 申请ID（外键 → borrow_request.id，级联删除，一对一）
- approver_id: 审批人ID（外键 → user.id，保护删除）
- decision: 决策（APPROVED/REJECTED）
- comment: 审批意见
- decided_at: 决策时间
- created_at: 创建时间
- updated_at: 更新时间
```

**return_record表**
```python
- id: 主键（bigint自增）
- borrow_item_id: 借用明细ID（外键 → borrow_item.id，级联删除，一对一）
- checked_by: 验收人ID（外键 → user.id，保护删除）
- condition: 归还状态（GOOD/DAMAGED/LOST）
- fee: 费用
- remark: 备注
- checked_at: 验收时间
- created_at: 创建时间
- updated_at: 更新时间
```

#### 3.2.4 维修管理模块

**maintenance表**
```python
- id: 主键（bigint自增）
- equipment_id: 设备ID（外键 → equipment.id，级联删除）
- title: 维修标题
- description: 维修描述
- status: 状态（OPEN/IN_PROGRESS/DONE）
- assigned_to: 分配给（外键 → user.id，保护删除，可为空）
- cost: 维修费用
- started_at: 开始时间
- finished_at: 完成时间
- created_at: 创建时间
- updated_at: 更新时间
- 索引: equipment_id, status
```

---

## 4. 状态机设计

### 4.1 设备状态机

**状态枚举**：AVAILABLE / RESERVED / BORROWED / OUT(legacy) / RETURN_PENDING / MAINTENANCE / DISCARDED

**当前借用主流程（已实现）**：
```
AVAILABLE → BORROWED → AVAILABLE
```

**状态流转规则**：
- AVAILABLE → BORROWED：管理员出库时
- BORROWED → AVAILABLE：管理员归还验收时
- AVAILABLE → MAINTENANCE：设备进入维修
- MAINTENANCE → AVAILABLE：维修完成
- 任意状态 → DISCARDED：设备报废（终态）

**状态变更接口（管理员）**：
- 支持按 `inventory.views._is_transition_allowed` 做校验流转
- 例如：MAINTENANCE → AVAILABLE；DISCARDED 为终态等

### 4.2 借用申请状态机

**状态枚举**：REQUESTED / APPROVED / REJECTED / OUT / CLOSED

**状态流转**：
```
REQUESTED → APPROVED → OUT → CLOSED
    ↓
REJECTED
```

**状态说明**：
- REQUESTED：已提交申请
- APPROVED：管理员审批通过
- REJECTED：管理员审批拒绝
- OUT：管理员已出库
- CLOSED：管理员已验收归还

### 4.3 借用明细状态机

**状态枚举**：PENDING / OUT / RETURN_PENDING / RETURN_ACCEPTED / RETURN_REJECTED

**状态流转**：
```
PENDING → OUT → RETURN_ACCEPTED（当前由管理员验收闭环）
```

**预留状态**：
- RETURN_PENDING：归还提交（暂未在接口中走到）
- RETURN_REJECTED：归还拒绝（暂未在接口中走到）

### 4.4 库存交易状态机

**状态枚举**：PENDING / APPROVED / REJECTED

**状态流转**：
```
PENDING → APPROVED
    ↓
REJECTED
```

**业务规则**：
- OUT 在管理员 APPROVED 时才扣减库存
- IN 直接 APPROVED，增加库存

### 4.5 维修状态机

**状态枚举**：OPEN / IN_PROGRESS / DONE

**状态流转**：
```
OPEN → IN_PROGRESS → DONE
```

**业务规则**：
- DONE 时设备可回 AVAILABLE

---

## 5. 关键业务规则

### 5.1 认证与授权
- **登录**：使用 SimpleJWT，所有接口除登录/刷新外需携带 Bearer token
- **RBAC**：请求需校验角色；管理员为唯一审批人

### 5.2 设备借用
- **创建申请**：校验设备为 AVAILABLE
- **审批**：管理员 approve 后 request→APPROVED（本阶段不改设备状态）
- **出库**：管理员 checkout 时事务 + 行锁，确保仅 AVAILABLE 可出库，并将设备→BORROWED、borrow_item→OUT、request→OUT，同时写入 equipment_status_log
- **归还验收**：管理员 return 时写 ReturnRecord，并将设备→AVAILABLE、borrow_item→RETURN_ACCEPTED、request→CLOSED，同时写入 equipment_status_log

### 5.3 维修
- **维修中不可出库**：设备进入 MAINTENANCE 时不可出库
- **维修完成**：维修完成方可回 AVAILABLE

### 5.4 耗材管理
- **入库**：管理员创建 stock_txn(IN,APPROVED)，并增加 current_stock（事务内更新）
- **领用**：学生/老师创建 stock_txn(OUT,PENDING)，不扣库存
- **审核**：管理员审核 APPROVED 时在事务内（行锁/F 表达式）扣减 current_stock，若库存不足则审核失败且不改动库存
- **库存预警**：current_stock < safety_stock 时标记预警或在列表标识

### 5.5 报表
- **借用排行**：按设备被借次数或时长排序
- **设备利用率**：OUT 时长 / 可用时长（按日志或状态变更）
- **耗材月消耗**：按 month(sum OUT.qty where status=APPROVED)

---

## 6. REST接口设计

### 6.1 分页约定
- 对 ViewSet 的列表接口生效（例如：`/equipment/`、`/equipment-categories/`、`/consumables/`、`/stock/`、`/maintenance/`、`/users/`、`/roles/`、`/user-roles/`、`/borrow/requests/`）
- 返回结构：`{count, next, previous, results}`
- 通过 `?page=1` 翻页

### 6.2 认证接口
- **POST** `/api/v1/auth/token/` body{username,password} → tokens{access,refresh}
- **POST** `/api/v1/auth/token/refresh/` body{refresh} → access

### 6.3 当前用户信息
- **GET** `/api/v1/me/`（登录用户）→ `{id, username, real_name, email, phone, roles, is_admin}`

### 6.4 用户与角色（管理员）
- **用户**：`/api/v1/users/`（CRUD）
- **启用/停用**：`PATCH /api/v1/users/{id}/activate/` / `PATCH /api/v1/users/{id}/deactivate/`
- **角色**：`/api/v1/roles/`（CRUD）
- **用户角色**：`/api/v1/user-roles/`（创建/删除）

### 6.5 设备台账
- **设备分类**：`/api/v1/equipment-categories/`（CRUD；非管理员只读）
- **设备台账**：`/api/v1/equipment/`（CRUD；支持 status/category/keyword 过滤 + 分页；非管理员只读）
- **状态变更**：`POST /api/v1/equipment/{id}/status/`（管理员）
- **状态日志**：`GET /api/v1/equipment/{id}/logs/`

### 6.6 借用流程（设备借用 v1，严格按状态机）
- **POST** `/api/v1/borrow/requests/`（学生/老师）→ 创建 borrow_request + borrow_item，状态 `REQUESTED`
- **POST** `/api/v1/borrow/requests/{id}/approve/`（管理员）→ `APPROVED`，写 `borrow_approval`
- **POST** `/api/v1/borrow/requests/{id}/reject/`（管理员）→ `REJECTED`，写 `borrow_approval`
- **POST** `/api/v1/borrow/requests/{id}/checkout/`（管理员）→ 事务 + 行锁；仅 `AVAILABLE` 可出库；设备→`BORROWED`；写 `equipment_status_log`；request→`OUT`
- **POST** `/api/v1/borrow/requests/{id}/return/`（管理员）→ 写 `return_record`；设备→`AVAILABLE`；写 `equipment_status_log`；request→`CLOSED`
- **GET** `/api/v1/borrow/requests/`（管理员全量；老师全量只读；学生仅自己）
- **GET** `/api/v1/borrow/requests/{id}/`

### 6.7 耗材库存
- **耗材**：`/api/v1/consumables/`（CRUD；非管理员只读）
- **预警**：`GET /api/v1/consumables/warnings/`
- **入库**：`POST /api/v1/stock/in/`（管理员；直接 APPROVED；增加库存）
- **领用申请**：`POST /api/v1/stock/out/`（学生/老师；OUT + PENDING；不扣库存）
- **审核**：`POST /api/v1/stock/{id}/approve/` / `POST /api/v1/stock/{id}/reject/`（管理员；approve 防负库存）
- **流水查询**：`GET /api/v1/stock/`（管理员全量；其余仅本人；分页）
- **GET** `/api/v1/stock/{id}/`

### 6.8 维修
- **维修单**：`/api/v1/maintenance/`（列表/创建/更新；写入需要管理员或 MAINTAINER 角色）

### 6.9 报表
- **GET** `/api/v1/reports/borrow-top/` params{limit,start?,end?}
- **GET** `/api/v1/reports/equipment-utilization/` params{start,end,limit?,include_zero?}
- **GET** `/api/v1/reports/consumable-monthly/` params{year?,consumable_id?}

### 6.10 请求/响应示例

**创建借用申请**
```json
POST /api/v1/borrow/requests/
{
  "purpose": "毕业课题实验",
  "expected_return_date": "2025-01-20",
  "items": [{"equipment_id":101},{"equipment_id":102}]
}
→ 201 { "id":9001, "status":"REQUESTED", "items":[...] }
```

**审批借用申请**
```json
POST /api/v1/borrow/requests/9001/approve/
{ "comment":"准予借用" }
→ 200 { "status":"APPROVED" }
```

**耗材入库**
```json
POST /api/v1/stock/in/
{ "consumable_id": 11, "qty": 50, "remark": "本月采购" }
→ 201 { "id": 1, "consumable_id": 11, "type": "IN", "status": "APPROVED", "qty": 50 }
```

**借用排行**
```json
GET /api/v1/reports/borrow-top/?limit=10
→ 200 {
  "limit": 10,
  "range": {"start": null, "end": null},
  "items": [{"equipment_id": 1, "equipment_code": "E-001", "equipment_name": "示波器", "borrow_count": 3}]
}
```

**设备利用率**
```json
GET /api/v1/reports/equipment-utilization/?start=2025-12-01&end=2025-12-31
→ 200 {
  "range": {"start":"2025-12-01","end":"2025-12-31","range_days":31},
  "items": [{"equipment_id": 1, "equipment_code": "E-001", "equipment_name": "示波器", "borrowed_days": 5, "range_days": 31, "utilization_rate": 0.16129}]
}
```

**耗材月消耗**
```json
GET /api/v1/reports/consumable-monthly/?year=2025
→ 200 {
  "year": 2025,
  "items": [{"month":"2025-01","qty":0},{"month":"2025-02","qty":12}]
}
```

**当前用户信息**
```json
GET /api/v1/me/
→ 200 {
  "id": 1,
  "username": "teacher1",
  "real_name": "张三",
  "email": "",
  "phone": "",
  "roles": ["TEACHER"],
  "is_admin": false
}
```

---

## 7. 前端页面与路由

### 7.1 页面清单（Vue3 + Vite + ElementPlus + Pinia）
- `/login` 登录页
- `/dashboard` 仪表盘（预警、今日审批、统计卡片）
- `/equipment` 设备台账列表；`/equipment/:id` 详情+状态日志+维修记录；`/equipment/categories` 分类管理（管理员）
- `/borrow/requests` 借用申请列表；`/borrow/new` 创建；`/borrow/:id` 详情+审批+出库+归还验收
- `/consumables` 耗材与库存（同页内包含：耗材 CRUD/预警/入库/领用申请/流水/审批等 Tab）
- `/maintenance` 维修单列表/编辑
- `/reports/borrow-top`，`/reports/utilization`，`/reports/consumption`
- `/admin/users` 用户管理；`/admin/roles` 角色管理

### 7.2 路由守卫
- 导航基于角色动态过滤
- 路由守卫校验 token & 角色

---

## 8. 软件系统实现

### 8.1 技术栈

#### 8.1.1 后端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.8+ | 编程语言 |
| Django | 4.x | Web框架 |
| Django REST Framework | 3.x | RESTful API框架 |
| SimpleJWT | 5.x | JWT认证 |
| MySQL | 8.0+ | 数据库 |
| PyMySQL | 1.x | MySQL驱动 |

#### 8.1.2 前端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Vue.js | 3.x | 前端框架 |
| Vite | 4.x | 构建工具 |
| TypeScript | 5.x | 类型系统 |
| Element Plus | 2.x | UI组件库 |
| Pinia | 2.x | 状态管理 |
| Axios | 1.x | HTTP客户端 |
| Vue Router | 4.x | 路由管理 |

### 8.2 后端模块设计

#### 8.2.1 项目结构
```
backend/
├── accounts/          # 用户与角色模块
│   ├── models.py      # 数据模型
│   ├── serializers.py # 序列化器
│   ├── views.py       # 视图
│   ├── urls.py        # 路由
│   └── permissions.py # 权限控制
├── inventory/         # 设备与库存模块
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── borrowing/         # 借用管理模块
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── maintenance/       # 维修管理模块
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── reports/           # 报表统计模块
│   ├── views.py
│   └── urls.py
└── project/           # 项目配置
    ├── settings.py    # 配置文件
    └── urls.py        # 主路由
```

#### 8.2.2 核心模块说明

**accounts模块**
- **功能**：用户管理、角色管理、认证授权
- **主要模型**：User、Role、UserRole
- **主要接口**：
  - POST /api/v1/auth/token/ - 用户登录
  - POST /api/v1/auth/token/refresh/ - 刷新Token
  - GET /api/v1/me/ - 获取当前用户信息
  - GET /api/v1/users/ - 获取用户列表
  - POST /api/v1/users/ - 创建用户
  - PATCH /api/v1/users/{id}/activate/ - 启用用户
  - PATCH /api/v1/users/{id}/deactivate/ - 停用用户
  - GET /api/v1/roles/ - 获取角色列表
  - POST /api/v1/roles/ - 创建角色
  - GET /api/v1/user-roles/ - 获取用户角色
  - POST /api/v1/user-roles/ - 分配角色

**inventory模块**
- **功能**：设备管理、耗材管理、库存管理
- **主要模型**：Equipment、EquipmentCategory、Consumable、StockTxn、EquipmentStatusLog
- **主要接口**：
  - GET /api/v1/equipment/ - 获取设备列表
  - POST /api/v1/equipment/ - 创建设备
  - PUT /api/v1/equipment/{id}/ - 更新设备
  - DELETE /api/v1/equipment/{id}/ - 删除设备
  - POST /api/v1/equipment/{id}/status/ - 变更设备状态
  - GET /api/v1/equipment/{id}/logs/ - 获取设备状态日志
  - GET /api/v1/equipment-categories/ - 获取设备分类列表
  - POST /api/v1/equipment-categories/ - 创建设备分类
  - GET /api/v1/consumables/ - 获取耗材列表
  - POST /api/v1/consumables/ - 创建耗材
  - GET /api/v1/consumables/warnings/ - 获取库存预警
  - POST /api/v1/stock/in/ - 耗材入库
  - POST /api/v1/stock/out/ - 耗材领用申请
  - GET /api/v1/stock/ - 获取库存流水
  - POST /api/v1/stock/{id}/approve/ - 审批领用
  - POST /api/v1/stock/{id}/reject/ - 拒绝领用

**borrowing模块**
- **功能**：借用申请、审批、出借、归还
- **主要模型**：BorrowRequest、BorrowItem、BorrowApproval、ReturnRecord
- **主要接口**：
  - GET /api/v1/borrow/requests/ - 获取借用申请列表
  - POST /api/v1/borrow/requests/ - 创建借用申请
  - GET /api/v1/borrow/requests/{id}/ - 获取申请详情
  - POST /api/v1/borrow/requests/{id}/approve/ - 审批申请
  - POST /api/v1/borrow/requests/{id}/reject/ - 拒绝申请
  - POST /api/v1/borrow/requests/{id}/checkout/ - 设备出库
  - POST /api/v1/borrow/requests/{id}/return/ - 设备归还验收

**maintenance模块**
- **功能**：维修管理
- **主要模型**：Maintenance
- **主要接口**：
  - GET /api/v1/maintenance/ - 获取维修列表
  - POST /api/v1/maintenance/ - 创建维修记录
  - GET /api/v1/maintenance/{id}/ - 获取维修详情
  - PUT /api/v1/maintenance/{id}/ - 更新维修记录
  - DELETE /api/v1/maintenance/{id}/ - 删除维修记录

**reports模块**
- **功能**：报表统计
- **主要接口**：
  - GET /api/v1/reports/borrow-top/ - 借用排行
  - GET /api/v1/reports/equipment-utilization/ - 设备利用率
  - GET /api/v1/reports/consumable-monthly/ - 耗材月消耗

### 8.3 前端模块设计

#### 8.3.1 项目结构
```
frontend/
├── src/
│   ├── api/           # API接口封装
│   ├── assets/        # 静态资源
│   ├── components/    # 公共组件
│   ├── config/        # 配置文件
│   ├── constants/     # 常量定义
│   ├── directives/    # 自定义指令
│   ├── layouts/       # 布局组件
│   ├── router/        # 路由配置
│   ├── stores/        # Pinia状态管理
│   ├── types/         # TypeScript类型定义
│   ├── utils/         # 工具函数
│   ├── views/         # 页面组件
│   ├── App.vue        # 根组件
│   └── main.ts        # 入口文件
├── index.html         # HTML模板
├── package.json       # 依赖配置
└── vite.config.ts     # Vite配置
```

#### 8.3.2 核心页面

**登录页面（/login）**
- 用户名/密码登录
- JWT Token存储
- 路由跳转

**仪表盘（/dashboard）**
- 库存预警统计
- 今日审批统计
- 设备借用统计
- 耗材消耗统计

**设备管理（/equipment）**
- 设备列表：展示所有设备，支持按分类、状态筛选
- 设备详情：查看设备详细信息、状态日志、维修记录
- 设备添加：添加新设备（管理员）
- 设备编辑：编辑设备信息（管理员）
- 设备状态：管理设备状态（管理员）
- 分类管理：设备分类CRUD（管理员）

**借用管理（/borrow）**
- 申请列表：展示所有借用申请（根据角色过滤）
- 申请详情：查看申请详细信息
- 申请创建：创建借用申请（学生/老师/管理员）
- 申请审批：审批借用申请（管理员）
- 设备出库：设备出库操作（管理员）
- 设备归还：设备归还验收（管理员）

**耗材管理（/consumables）**
- 耗材列表：展示所有耗材
- 耗材详情：查看耗材详细信息
- 库存管理：管理耗材库存（管理员）
- 库存预警：查看低于安全库存的耗材
- 入库记录：耗材入库（管理员）
- 领用申请：提交耗材领用申请（学生/老师/管理员）
- 领用审核：审核领用申请（管理员）
- 出入库记录：查看库存交易记录

**维修管理（/maintenance）**
- 维修列表：展示所有维修记录
- 维修详情：查看维修详细信息
- 维修创建：创建维修记录（管理员/维修员）
- 维修更新：更新维修状态（管理员/维修员）

**报表统计（/reports）**
- 借用排行：展示借用数据统计（管理员/老师）
- 设备利用率：展示设备利用率统计（管理员/老师）
- 耗材月消耗：展示耗材月消耗统计（管理员/老师）

**系统管理（/admin）**
- 用户管理：用户CRUD、角色分配（管理员）
- 角色管理：角色CRUD（管理员）

#### 8.3.3 状态管理

使用Pinia进行状态管理，主要Store：
- **userStore**：用户信息、登录状态、用户角色
- **equipmentStore**：设备列表、筛选条件
- **borrowStore**：借用申请列表、审批状态
- **consumableStore**：耗材列表、库存信息
- **maintenanceStore**：维修列表、维修状态

### 8.4 部署与运维

#### 8.4.1 环境配置
- **开发环境**：使用.env文件配置环境变量
- **生产环境**：使用环境变量或配置中心
- **数据库配置**：MySQL连接配置
- **日志配置**：Django日志系统

#### 8.4.2 部署方案
- **后端部署**：使用Gunicorn + Nginx
- **前端部署**：使用Vite构建，Nginx托管静态文件
- **数据库部署**：MySQL主从复制或集群
- **缓存部署**：Redis缓存热点数据

#### 8.4.3 监控与维护
- **日志监控**：收集和分析系统日志
- **性能监控**：监控系统性能指标
- **错误监控**：捕获和跟踪错误
- **数据备份**：定期备份数据库

---

## 9. 数据库范式分析

### 9.1 范式评估

#### 9.1.1 第一范式（1NF）
✅ **完全符合**

**定义**：每个字段都是不可再分的最小数据单位，表中的每一列都是原子的。

**验证**：
- 所有13张表的字段都是原子的，没有复合字段
- 没有重复组或多值属性
- 每个字段都有明确的数据类型和约束

#### 9.1.2 第二范式（2NF）
✅ **完全符合**

**定义**：在满足1NF的基础上，非主键字段完全依赖于主键，不存在部分依赖。

**验证**：
- 所有表都有明确的主键（id字段）
- 所有非主键字段都完全依赖于主键
- 没有复合主键，因此不存在部分依赖问题

#### 9.1.3 第三范式（3NF）
✅ **95%符合**

**定义**：在满足2NF的基础上，非主键字段不传递依赖于主键。

**验证**：
- 大部分表完全符合3NF
- **例外情况**：`borrow_request`表包含`approver_id`、`approved_at`、`rejected_at`字段
  - 这些字段可以通过`borrow_approval`表获取
  - 存在轻微的传递依赖：`borrow_request.id → borrow_approval.approver_id → borrow_request.approver_id`
  - **设计理由**：这是有意的设计，目的是提高查询效率和简化业务逻辑

### 9.2 设计亮点

#### 9.2.1 外键设计
- **CASCADE删除**：用于强关联关系，如UserRole表
- **PROTECT删除**：用于保护重要数据，如Equipment表
- **SET_NULL删除**：用于可选关联，如StockTxn表的related_request字段

#### 9.2.2 索引优化
- **复合索引**：针对高频查询组合，如BorrowRequest表的`(applicant_id, status, type)`索引
- **单列索引**：针对状态字段，如Equipment表的status索引
- **唯一索引**：确保数据唯一性，如UserRole表的唯一约束

#### 9.2.3 约束完善
- **唯一约束**：确保关键字段的唯一性
- **非空约束**：关键字段设置非空约束
- **外键约束**：保证数据引用完整性

#### 9.2.4 时间戳统一
- 所有表包含`created_at`和`updated_at`字段
- 统一的时间戳管理，便于追踪数据变更

### 9.3 优化建议

#### 9.3.1 数据一致性
- **触发器**：可以使用数据库触发器保持`borrow_request`与`borrow_approval`表的一致性
- **应用层逻辑**：在应用层实现事务，确保数据一致性

#### 9.3.2 性能优化
- **添加索引**：为高频查询字段添加更多索引
- **查询优化**：使用EXPLAIN分析慢查询，优化SQL语句
- **缓存策略**：对热点数据使用缓存

#### 9.3.3 扩展性
- **分区表**：对大表进行分区，提高查询性能
- **读写分离**：实现数据库读写分离，提高并发能力
- **分库分表**：根据业务增长情况，考虑分库分表

---

## 10. 数据库引擎配置

### 10.1 数据库类型

**MySQL数据库**

从`backend/project/settings.py`配置可以看到：
```python
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": os.getenv("MYSQL_DATABASE", "lab_manage"),
        "USER": os.getenv("MYSQL_USER", "root"),
        "PASSWORD": os.getenv("MYSQL_PASSWORD", ""),
        "HOST": os.getenv("MYSQL_HOST", "127.0.0.1"),
        "PORT": os.getenv("MYSQL_PORT", "3306"),
        "OPTIONS": {"charset": "utf8mb4"},
    }
}
```

### 10.2 存储引擎

**InnoDB引擎**

#### 10.2.1 为什么使用InnoDB？

1. **MySQL默认引擎**
   - 从MySQL 5.5.5版本开始，InnoDB已成为MySQL的默认存储引擎
   - Django创建表时，如果没有显式指定引擎，会使用MySQL服务器的默认引擎

2. **外键约束要求**
   - 项目使用了大量外键关系，必须使用InnoDB引擎
   - MyISAM引擎不支持外键约束

3. **事务支持**
   - 业务逻辑涉及复杂的借用流程和库存管理
   - 需要事务支持来保证数据一致性
   - InnoDB支持ACID事务特性

4. **行级锁**
   - InnoDB支持行级锁，提高并发性能
   - MyISAM只支持表级锁，并发性能较差

5. **崩溃恢复**
   - InnoDB有完善的崩溃恢复机制
   - 保证数据不会因为系统崩溃而丢失

#### 10.2.2 InnoDB特性

**事务支持（ACID）**
- **原子性（Atomicity）**：事务中的操作要么全部成功，要么全部失败
- **一致性（Consistency）**：事务执行前后，数据库从一个一致性状态转换到另一个一致性状态
- **隔离性（Isolation）**：并发执行的事务之间互不干扰
- **持久性（Durability）**：事务提交后，对数据库的修改是永久的

**外键约束**
- 确保数据引用完整性
- 防止孤儿记录
- 支持级联操作（CASCADE、SET NULL等）

**行级锁**
- 提高并发性能
- 减少锁冲突
- 支持高并发场景

**崩溃恢复**
- 使用redo log和undo log
- 保证数据不丢失
- 快速恢复

### 10.3 字符集配置

**utf8mb4字符集**

```python
"OPTIONS": {"charset": "utf8mb4"}
```

**为什么使用utf8mb4？**

1. **完整Unicode支持**
   - 支持所有Unicode字符，包括emoji
   - utf8只能存储3字节的字符，utf8mb4可以存储4字节的字符

2. **向后兼容**
   - utf8mb4是utf8的超集
   - 完全兼容utf8编码

3. **国际化支持**
   - 支持多语言
   - 适合国际化应用

### 10.4 数据库配置总结

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 数据库类型 | MySQL | 关系型数据库 |
| 存储引擎 | InnoDB | 默认引擎，支持事务和外键 |
| 字符集 | utf8mb4 | 支持完整Unicode |
| 排序规则 | utf8mb4_unicode_ci | 不区分大小写排序 |
| 连接方式 | TCP/IP | 网络连接 |
| 默认端口 | 3306 | MySQL默认端口 |

---

## 11. 演示脚本

### 11.1 完整业务流程演示

1. **管理员登录**
   - 使用管理员账号登录系统

2. **用户创建设备借用申请并提交**
   - 学生/老师/管理员登录
   - 选择需要借用的设备
   - 填写借用用途和预期归还日期
   - 提交借用申请

3. **管理员审批通过**
   - 管理员查看待审批申请
   - 审批通过（request 状态变 APPROVED）
   - 添加审批意见

4. **管理员出库**
   - 管理员执行出库操作
   - request→OUT；设备→BORROWED
   - 查看状态日志

5. **管理员归还验收**
   - 管理员执行归还验收操作
   - request→CLOSED；设备回 AVAILABLE
   - 记录归还状态（完好/损坏/丢失）

6. **管理员登记设备维修，完结维修**
   - 管理员为故障设备创建维修记录
   - 分配维修员
   - 更新维修状态（OPEN → IN_PROGRESS → DONE）
   - 维修完成后设备回 AVAILABLE

7. **管理员为耗材入库；用户提交耗材领用申请；管理员审核扣减库存，查看预警**
   - 管理员执行耗材入库操作
   - 学生/老师提交耗材领用申请
   - 管理员审核领用申请
   - 查看库存预警

8. **查看报表**
   - 借用排行
   - 设备利用率
   - 耗材月消耗

---

## 12. 测试用例要点

### 12.1 认证测试
- 登录/刷新；过期 token 拒绝

### 12.2 RBAC测试
- 普通用户无法访问审批/出库接口
- 未授权路由被拦截

### 12.3 设备借用测试
- 仅 AVAILABLE 可加入申请
- approve 后 request=APPROVED
- checkout 时事务更新设备(BORROWED)+借用行(OUT)+request(OUT)
- 归还验收后：request=CLOSED、borrow_item=RETURN_ACCEPTED、return_record 生成、设备回 AVAILABLE
- MAINTENANCE 状态设备不可出库

### 12.4 耗材测试
- 入库增加库存
- 审批时扣减库存防负数（并发下事务正确）
- 安全库存预警触发

### 12.5 维修测试
- 设备进入 MAINTENANCE 后禁止借出
- 完成后可借

### 12.6 报表测试
- 数据范围过滤正确
- 借用排行/利用率计算正确

### 12.7 并发一致性测试
- 出库/扣库存使用事务+行锁，F 表达式验证无脏写

### 12.8 数据完整性测试
- 唯一约束/外键/索引验证
- 删除被引用记录受限或使用软删

### 12.9 API契约测试
- 请求校验、错误码、分页排序、生效过滤

---

## 13. 总结

本项目是一个完整的实验室设备与耗材借用管理系统，具有以下特点：

### 13.1 技术特点
- **前后端分离**：Vue3 + Django REST Framework
- **RESTful API**：标准化的API接口设计（/api/v1/）
- **JWT认证**：安全的用户认证机制（SimpleJWT）
- **数据库设计**：规范的数据库设计，符合3NF
- **权限控制**：基于角色的访问控制（RBAC）

### 13.2 业务特点
- **完整流程**：申请-审批-出借-归还验收全流程管理
- **状态管理**：完善的状态机设计
- **库存管理**：耗材库存监控和预警
- **维修管理**：设备维修全流程跟踪
- **数据统计**：丰富的报表统计功能

### 13.3 数据库特点
- **13张业务表**：覆盖所有业务场景
- **外键约束**：保证数据完整性
- **索引优化**：提高查询性能
- **时间戳统一**：便于追踪数据变更
- **InnoDB引擎**：支持事务和外键

### 13.4 扩展性
- **模块化设计**：前后端模块化，便于扩展
- **灵活的状态机**：易于添加新的状态
- **可扩展的角色系统**：支持添加新角色
- **丰富的API**：便于集成其他系统

---

## 附录

### A. 项目文件结构
```
final_homework/
├── backend/              # 后端项目
│   ├── accounts/         # 用户与角色模块
│   ├── borrowing/        # 借用管理模块
│   ├── inventory/        # 设备与库存模块
│   ├── maintenance/      # 维修管理模块
│   ├── reports/          # 报表统计模块
│   └── project/          # 项目配置
├── frontend/             # 前端项目
│   ├── src/
│   │   ├── api/          # API接口
│   │   ├── components/   # 公共组件
│   │   ├── views/        # 页面组件
│   │   └── router/       # 路由配置
│   └── package.json
├── docs/                 # 项目文档
│   ├── SPEC.md          # 功能规范
│   ├── ENV.md           # 环境配置
│   └── STATUS.md        # 开发状态
├── README.md            # 项目说明
└── 项目说明文档.md       # 本文档
```

### B. 快速开始

#### B.1 后端启动
```bash
cd backend
pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

#### B.2 前端启动
```bash
cd frontend
npm install
npm run dev
```

#### B.3 数据库初始化
```bash
# 创建数据库
mysql -u root -p
CREATE DATABASE lab_manage CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 运行迁移
python manage.py migrate

# 创建超级用户
python manage.py createsuperuser
```

### C. 常见问题

**Q: 如何添加新的角色？**
A: 在数据库的role表中添加新记录，或在Django Admin中创建。

**Q: 如何修改设备状态？**
A: 通过API接口修改equipment表的status字段，系统会自动记录状态变更日志。

**Q: 如何备份和恢复数据库？**
A: 使用mysqldump备份，使用mysql命令恢复。

**Q: 如何查看系统日志？**
A: 日志文件位于backend/logs/目录（如果配置了日志）。

**Q: 老师角色可以审批借用申请吗？**
A: 不可以。根据设计规范，只有管理员（ADMIN）可以审批借用申请、出库和归还验收。老师（TEACHER）只能查看全量借用单（只读）和统计报表（只读），不能进行审批、出库、验收等操作。

**Q: 维修员角色可以做什么？**
A: 维修员（MAINTAINER）可以创建/更新维修单（写入），其余功能只读。如果没有维修员，管理员可以兼任。

**Q: 学生角色可以查看哪些数据？**
A: 学生（STUDENT）可以发起设备借用/耗材领用，仅可查看本人单据，不可审批/出入库/验收。

**Q: 如何重置用户密码？**
A: 可以通过Django Shell使用set_password方法重置密码，或通过管理员在用户管理页面重置密码。

---

