# 实验室设备与耗材借用管理系统 - 项目说明文档

## 目录
- [1. 项目概述](#1-项目概述)
- [2. 数据库设计](#2-数据库设计)
- [3. 角色模式与功能](#3-角色模式与功能)
- [4. 数据库设计流程](#4-数据库设计流程)
- [5. 软件系统实现](#5-软件系统实现)
- [6. 数据库范式分析](#6-数据库范式分析)
- [7. 数据库引擎配置](#7-数据库引擎配置)

---

## 1. 项目概述

### 1.1 项目简介
本项目是一个完整的实验室设备与耗材借用管理系统，旨在实现实验室资源的数字化管理，提高设备借用效率和耗材库存管理水平。

### 1.2 核心功能模块
- **用户与角色管理**：多角色权限控制
- **设备借用流程**：申请、审批、出借、归还全流程管理
- **耗材与库存管理**：库存监控、出入库管理
- **设备维修管理**：维修申请、处理、记录
- **报表统计**：数据统计与分析

### 1.3 技术架构
- **后端**：Django + DRF + SimpleJWT + MySQL
- **前端**：Vue3 + Vite + ElementPlus + Pinia + Axios
- **认证**：JWT Token认证
- **数据库**：MySQL（InnoDB引擎）

---

## 2. 数据库设计

### 2.1 数据库表概览
系统共包含**13张业务数据表**，分为4个模块：

#### 2.1.1 用户与角色模块（3张表）
| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `user` | 用户表 | username, real_name, phone, created_at, updated_at |
| `role` | 角色表 | code, name, created_at, updated_at |
| `user_role` | 用户角色关联表 | user_id, role_id（唯一约束） |

#### 2.1.2 设备与库存模块（5张表）
| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `equipment_category` | 设备分类表 | name, code, description |
| `equipment` | 设备表 | code, name, category_id, status, location, price |
| `equipment_status_log` | 设备状态日志表 | equipment_id, from_status, to_status, changed_by |
| `consumable` | 耗材表 | code, name, unit, safety_stock, current_stock |
| `stock_txn` | 库存交易表 | consumable_id, type, status, qty, performed_by |

#### 2.1.3 借用管理模块（4张表）
| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `borrow_request` | 借用申请表 | applicant_id, type, status, purpose, expected_return_date |
| `borrow_item` | 借用明细表 | request_id, item_type, equipment_id, consumable_id, qty, status |
| `borrow_approval` | 借用审批表 | request_id, approver_id, decision, comment |
| `return_record` | 归还记录表 | borrow_item_id, checked_by_id, condition, fee |

#### 2.1.4 维修管理模块（1张表）
| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `maintenance` | 维修记录表 | equipment_id, title, description, status, assigned_to, cost |

### 2.2 表结构详细说明

#### 2.2.1 用户与角色模块

**user表**
```python
- id: 主键
- username: 用户名（继承自AbstractUser）
- password: 密码（加密存储）
- real_name: 真实姓名
- phone: 联系电话
- created_at: 创建时间
- updated_at: 更新时间
```

**role表**
```python
- id: 主键
- code: 角色编码（唯一）
- name: 角色名称
- created_at: 创建时间
- updated_at: 更新时间
```

**user_role表**
```python
- id: 主键
- user_id: 用户ID（外键 → user.id，级联删除）
- role_id: 角色ID（外键 → role.id，级联删除）
- created_at: 创建时间
- updated_at: 更新时间
- 约束: 唯一约束 (user_id, role_id)
- 索引: user_id, role_id
```

#### 2.2.2 设备与库存模块

**equipment_category表**
```python
- id: 主键
- name: 分类名称（唯一）
- code: 分类编码（唯一）
- description: 描述
- created_at: 创建时间
- updated_at: 更新时间
```

**equipment表**
```python
- id: 主键
- code: 设备编码（唯一）
- name: 设备名称
- category_id: 分类ID（外键 → equipment_category.id，保护删除）
- status: 状态（AVAILABLE/RESERVED/BORROWED/OUT/RETURN_PENDING/MAINTENANCE/DISCARDED）
- location: 存放位置
- purchase_date: 购买日期
- price: 价格
- vendor: 供应商
- spec: 规格
- usable: 是否可用
- created_at: 创建时间
- updated_at: 更新时间
- 索引: category_id, status
```

**equipment_status_log表**
```python
- id: 主键
- equipment_id: 设备ID（外键 → equipment.id，级联删除）
- from_status: 原状态
- to_status: 新状态
- changed_by: 操作人ID（外键 → user.id，保护删除）
- reason: 原因
- remark: 备注
- created_at: 创建时间
- updated_at: 更新时间
- 索引: equipment_id
```

**consumable表**
```python
- id: 主键
- code: 耗材编码（唯一）
- name: 耗材名称
- unit: 单位
- category: 分类
- safety_stock: 安全库存
- current_stock: 当前库存
- location: 存放位置
- created_at: 创建时间
- updated_at: 更新时间
- 索引: code, current_stock
```

**stock_txn表**
```python
- id: 主键
- consumable_id: 耗材ID（外键 → consumable.id，级联删除）
- type: 交易类型（IN/OUT）
- status: 状态（PENDING/APPROVED/REJECTED）
- qty: 数量
- related_request_id: 关联申请ID（外键 → borrow_request.id，置空删除）
- performed_by: 操作人ID（外键 → user.id，保护删除）
- reviewed_by: 审核人ID（外键 → user.id，保护删除，可为空）
- decided_at: 决策时间
- remark: 备注
- created_at: 创建时间
- updated_at: 更新时间
- 索引: (consumable_id, type, created_at), status
```

#### 2.2.3 借用管理模块

**borrow_request表**
```python
- id: 主键
- applicant_id: 申请人ID（外键 → user.id，保护删除）
- type: 申请类型（EQUIPMENT/CONSUMABLE）
- status: 状态（REQUESTED/APPROVED/REJECTED/OUT/CLOSED）
- purpose: 用途
- expected_return_date: 预期归还日期
- submitted_at: 提交时间
- approved_at: 审批通过时间
- rejected_at: 审批拒绝时间
- approver_id: 审批人ID（外键 → user.id，保护删除，可为空）
- remark: 备注
- created_at: 创建时间
- updated_at: 更新时间
- 索引: (applicant_id, status, type)
```

**borrow_item表**
```python
- id: 主键
- request_id: 申请ID（外键 → borrow_request.id，级联删除）
- item_type: 物品类型（EQUIPMENT/CONSUMABLE）
- equipment_id: 设备ID（外键 → equipment.id，保护删除，可为空）
- consumable_id: 耗材ID（外键 → consumable.id，保护删除，可为空）
- qty: 数量
- status: 状态（PENDING/OUT/RETURN_PENDING/RETURN_ACCEPTED/RETURN_REJECTED）
- out_at: 出借时间
- due_at: 应归还时间
- return_submitted_at: 归还提交时间
- return_checked_at: 归还验收时间
- created_at: 创建时间
- updated_at: 更新时间
- 约束: 唯一约束 (request_id, equipment_id)
- 索引: (request_id, status)
```

**borrow_approval表**
```python
- id: 主键
- request_id: 申请ID（外键 → borrow_request.id，级联删除，一对一）
- approver_id: 审批人ID（外键 → user.id，保护删除）
- decision: 决策（APPROVED/REJECTED）
- comment: 审批意见
- decided_at: 决策时间
- created_at: 创建时间
- updated_at: 更新时间
```

**return_record表**
```python
- id: 主键
- borrow_item_id: 借用明细ID（外键 → borrow_item.id，级联删除，一对一）
- checked_by_id: 验收人ID（外键 → user.id，保护删除）
- condition: 归还状态（GOOD/DAMAGED/LOST）
- fee: 费用
- remark: 备注
- checked_at: 验收时间
- created_at: 创建时间
- updated_at: 更新时间
```

#### 2.2.4 维修管理模块

**maintenance表**
```python
- id: 主键
- equipment_id: 设备ID（外键 → equipment.id，保护删除）
- title: 维修标题
- description: 维修描述
- status: 状态（OPEN/IN_PROGRESS/DONE）
- assigned_to: 分配给（外键 → user.id，保护删除，可为空）
- cost: 维修费用
- started_at: 开始时间
- finished_at: 完成时间
- created_at: 创建时间
- updated_at: 更新时间
- 索引: equipment_id, status
```

---

## 3. 角色模式与功能

### 3.1 角色概览
系统定义了四种角色，每种角色具有不同的权限和功能：

| 角色 | 角色编码 | 主要职责 |
|------|----------|----------|
| 学生 | STUDENT | 申请借用设备、申请耗材 |
| 教师 | TEACHER | 审批借用申请、管理设备 |
| 维修员 | MAINTENANCE | 处理设备维修 |
| 系统管理员 | ADMIN | 系统配置、用户管理、数据维护 |

### 3.2 学生角色功能

#### 3.2.1 设备借用
- **查看设备列表**：浏览所有可用设备，查看设备详情（分类、位置、规格等）
- **提交借用申请**：填写借用用途、预期归还日期，选择需要借用的设备
- **查看申请状态**：实时跟踪申请状态（已提交、已批准、已拒绝、已出借、已关闭）
- **归还设备**：提交归还申请，等待验收

#### 3.2.2 耗材申请
- **查看耗材库存**：浏览耗材列表，查看当前库存和安全库存
- **提交耗材申请**：填写申请数量和用途
- **查看申请状态**：跟踪耗材申请的审批进度

#### 3.2.3 个人信息管理
- **查看个人信息**：查看个人资料、借用记录
- **修改个人信息**：更新联系方式等基本信息

### 3.3 教师角色功能

#### 3.3.1 借用审批
- **查看待审批申请**：查看所有待审批的借用申请
- **审批设备借用**：批准或拒绝学生的设备借用申请，添加审批意见
- **审批耗材申请**：批准或拒绝学生的耗材申请
- **查看审批历史**：查看历史审批记录

#### 3.3.2 设备出借与归还
- **设备出借**：将已批准的设备出借给学生，记录出借时间
- **设备归还验收**：验收归还的设备，记录归还状态（完好/损坏/丢失）和费用

#### 3.3.3 设备管理
- **查看设备列表**：查看所有设备及其状态
- **添加设备**：录入新设备信息（编码、名称、分类、位置、价格等）
- **编辑设备**：修改设备信息
- **设备状态管理**：手动更改设备状态（可用、维修中、报废等）

#### 3.3.4 耗材管理
- **查看耗材库存**：查看所有耗材的当前库存
- **耗材入库**：记录耗材入库操作，增加库存
- **耗材出库**：记录耗材出库操作，减少库存
- **库存预警**：查看低于安全库存的耗材

### 3.4 维修员角色功能

#### 3.4.1 维修管理
- **查看维修列表**：查看所有维修请求和维修记录
- **创建维修记录**：为故障设备创建维修记录
- **更新维修状态**：更新维修进度（待处理、进行中、已完成）
- **记录维修费用**：记录维修产生的费用
- **完成维修**：标记维修完成，记录完成时间

#### 3.4.2 设备查看
- **查看设备信息**：查看需要维修设备的详细信息
- **查看维修历史**：查看设备的维修历史记录

### 3.5 系统管理员角色功能

#### 3.5.1 用户管理
- **查看用户列表**：查看所有系统用户
- **添加用户**：创建新用户账号
- **编辑用户**：修改用户信息
- **删除用户**：删除用户账号
- **分配角色**：为用户分配角色（一个用户可以有多个角色）

#### 3.5.2 角色管理
- **查看角色列表**：查看所有系统角色
- **添加角色**：创建新角色
- **编辑角色**：修改角色信息
- **删除角色**：删除角色（需确保无用户使用该角色）

#### 3.5.3 系统配置
- **数据库备份**：定期备份数据库
- **系统监控**：监控系统运行状态
- **日志查看**：查看系统操作日志

#### 3.5.4 数据维护
- **数据清理**：清理过期数据
- **数据统计**：生成统计报表
- **数据导出**：导出系统数据

### 3.6 角色权限矩阵

| 功能模块 | 学生 | 教师 | 维修员 | 管理员 |
|----------|------|------|--------|--------|
| 查看设备列表 | ✓ | ✓ | ✓ | ✓ |
| 提交借用申请 | ✓ | - | - | - |
| 审批借用申请 | - | ✓ | - | ✓ |
| 设备出借 | - | ✓ | - | ✓ |
| 设备归还验收 | - | ✓ | - | ✓ |
| 添加/编辑设备 | - | ✓ | - | ✓ |
| 查看耗材库存 | ✓ | ✓ | - | ✓ |
| 提交耗材申请 | ✓ | - | - | - |
| 审批耗材申请 | - | ✓ | - | ✓ |
| 耗材入库/出库 | - | ✓ | - | ✓ |
| 创建维修记录 | - | - | ✓ | ✓ |
| 更新维修状态 | - | - | ✓ | ✓ |
| 用户管理 | - | - | - | ✓ |
| 角色管理 | - | - | - | ✓ |
| 系统配置 | - | - | - | ✓ |

---

## 4. 数据库设计流程

### 4.1 需求分析

#### 4.1.1 功能需求
1. **用户管理**：支持多角色用户，灵活的权限分配
2. **设备管理**：设备信息管理、状态跟踪、分类管理
3. **借用流程**：完整的申请-审批-出借-归还流程
4. **耗材管理**：库存监控、出入库管理、安全库存预警
5. **维修管理**：维修申请、处理、记录
6. **报表统计**：借用统计、库存统计、维修统计

#### 4.1.2 非功能需求
1. **数据一致性**：外键约束、事务支持
2. **查询性能**：合理的索引设计
3. **数据完整性**：唯一约束、非空约束
4. **可扩展性**：灵活的状态机设计
5. **安全性**：用户认证、权限控制

#### 4.1.3 业务规则
1. **审批规则**：管理员是唯一的审批人
2. **状态流转**：设备状态必须按照预定义的状态机流转
3. **库存管理**：耗材申请需要审批，出库需要审核
4. **操作日志**：关键操作需要记录日志
5. **借用限制**：同一设备在同一申请中只能出现一次

### 4.2 概念结构设计

#### 4.2.1 实体关系图（ER图）

**核心实体**：
- User（用户）
- Role（角色）
- Equipment（设备）
- EquipmentCategory（设备分类）
- Consumable（耗材）
- BorrowRequest（借用申请）
- BorrowItem（借用明细）
- BorrowApproval（借用审批）
- ReturnRecord（归还记录）
- Maintenance（维修记录）
- EquipmentStatusLog（设备状态日志）
- StockTxn（库存交易）

**主要关系**：
1. User ↔ Role：多对多关系（通过UserRole中间表）
2. Equipment → EquipmentCategory：多对一关系
3. Equipment → EquipmentStatusLog：一对多关系
4. BorrowRequest → User：多对一关系（申请人）
5. BorrowRequest → BorrowItem：一对多关系
6. BorrowRequest → BorrowApproval：一对一关系
7. BorrowItem → Equipment：多对一关系（可为空）
8. BorrowItem → Consumable：多对一关系（可为空）
9. BorrowItem → ReturnRecord：一对一关系
10. Consumable → StockTxn：一对多关系
11. Equipment → Maintenance：一对多关系

#### 4.2.2 状态机设计

**设备状态机**：
```
AVAILABLE → RESERVED → BORROWED → AVAILABLE
    ↓           ↓          ↓
MAINTENANCE ←─────────────┘
    ↓
DISCARDED
```

**借用申请状态机**：
```
REQUESTED → APPROVED → OUT → CLOSED
    ↓
REJECTED
```

**借用明细状态机**：
```
PENDING → OUT → RETURN_PENDING → RETURN_ACCEPTED
                    ↓
               RETURN_REJECTED
```

**库存交易状态机**：
```
PENDING → APPROVED
    ↓
REJECTED
```

**维修状态机**：
```
OPEN → IN_PROGRESS → DONE
```

### 4.3 逻辑结构设计

#### 4.3.1 表结构设计原则
1. **规范化**：遵循第三范式（3NF），减少数据冗余
2. **完整性**：使用外键约束、唯一约束、非空约束
3. **性能优化**：合理设计索引，优化查询性能
4. **可扩展性**：使用枚举类型管理状态，便于扩展

#### 4.3.2 字段类型选择
- **主键**：使用自增整数（BigAutoField）
- **外键**：使用整数类型，设置适当的删除策略
- **字符串**：根据实际长度选择CharField
- **文本**：使用TextField存储长文本
- **日期时间**：使用DateTimeField，设置默认值
- **金额**：使用DecimalField，指定精度
- **布尔值**：使用BooleanField
- **枚举**：使用TextChoices定义状态枚举

#### 4.3.3 索引设计
1. **外键索引**：所有外键字段自动创建索引
2. **复合索引**：针对高频查询组合创建复合索引
3. **唯一索引**：针对唯一约束字段创建唯一索引
4. **状态索引**：针对状态字段创建索引，优化状态查询

#### 4.3.4 约束设计
1. **外键约束**：使用CASCADE、PROTECT、SET_NULL等策略
2. **唯一约束**：确保关键字段的唯一性
3. **非空约束**：关键字段设置非空约束
4. **检查约束**：通过模型层实现业务规则验证

### 4.4 物理设计与实现

#### 4.4.1 数据库选择
- **数据库类型**：MySQL
- **存储引擎**：InnoDB（支持事务、外键约束）
- **字符集**：utf8mb4（支持完整Unicode）
- **排序规则**：utf8mb4_unicode_ci

#### 4.4.2 表物理设计
- **表名**：使用小写字母和下划线，符合命名规范
- **字段名**：使用小写字母和下划线，语义清晰
- **时间戳**：所有表包含created_at和updated_at字段
- **软删除**：重要数据使用软删除策略（如设置deleted_at字段）

#### 4.4.3 索引优化
1. **单列索引**：针对高频查询的单列字段
2. **复合索引**：针对多列组合查询
3. **覆盖索引**：优化常用查询，减少回表
4. **索引选择性**：确保索引字段具有高选择性

#### 4.4.4 分区设计
当前数据量较小，暂不需要分区设计。未来可根据数据增长情况考虑：
- **按时间分区**：针对日志类表（equipment_status_log）
- **按状态分区**：针对状态频繁变化的表

#### 4.4.5 Django ORM实现
使用Django ORM实现数据库模型，优势：
1. **类型安全**：编译时类型检查
2. **自动迁移**：自动生成数据库迁移脚本
3. **关系映射**：自动处理外键关系
4. **查询优化**：提供select_related、prefetch_related等优化方法

---

## 5. 软件系统实现

### 5.1 技术栈

#### 5.1.1 后端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.8+ | 编程语言 |
| Django | 4.x | Web框架 |
| Django REST Framework | 3.x | RESTful API框架 |
| SimpleJWT | 5.x | JWT认证 |
| MySQL | 8.0+ | 数据库 |
| PyMySQL | 1.x | MySQL驱动 |

#### 5.1.2 前端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Vue.js | 3.x | 前端框架 |
| Vite | 4.x | 构建工具 |
| TypeScript | 5.x | 类型系统 |
| Element Plus | 2.x | UI组件库 |
| Pinia | 2.x | 状态管理 |
| Axios | 1.x | HTTP客户端 |
| Vue Router | 4.x | 路由管理 |

### 5.2 后端模块设计

#### 5.2.1 项目结构
```
backend/
├── accounts/          # 用户与角色模块
│   ├── models.py      # 数据模型
│   ├── serializers.py # 序列化器
│   ├── views.py       # 视图
│   ├── urls.py        # 路由
│   └── permissions.py # 权限控制
├── inventory/         # 设备与库存模块
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── borrowing/         # 借用管理模块
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── maintenance/       # 维修管理模块
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── reports/           # 报表统计模块
│   ├── views.py
│   └── urls.py
└── project/           # 项目配置
    ├── settings.py    # 配置文件
    └── urls.py        # 主路由
```

#### 5.2.2 核心模块说明

**accounts模块**
- **功能**：用户管理、角色管理、认证授权
- **主要模型**：User、Role、UserRole
- **主要接口**：
  - POST /api/auth/login/ - 用户登录
  - POST /api/auth/logout/ - 用户登出
  - GET /api/users/ - 获取用户列表
  - POST /api/users/ - 创建用户
  - GET /api/roles/ - 获取角色列表
  - POST /api/roles/ - 创建角色

**inventory模块**
- **功能**：设备管理、耗材管理、库存管理
- **主要模型**：Equipment、EquipmentCategory、Consumable、StockTxn
- **主要接口**：
  - GET /api/equipment/ - 获取设备列表
  - POST /api/equipment/ - 创建设备
  - PUT /api/equipment/{id}/ - 更新设备
  - GET /api/consumables/ - 获取耗材列表
  - POST /api/consumables/ - 创建耗材
  - POST /api/stock-txn/ - 创建库存交易

**borrowing模块**
- **功能**：借用申请、审批、出借、归还
- **主要模型**：BorrowRequest、BorrowItem、BorrowApproval、ReturnRecord
- **主要接口**：
  - GET /api/borrow-requests/ - 获取借用申请列表
  - POST /api/borrow-requests/ - 创建借用申请
  - POST /api/borrow-requests/{id}/approve/ - 审批申请
  - POST /api/borrow-requests/{id}/out/ - 设备出借
  - POST /api/borrow-requests/{id}/return/ - 设备归还

**maintenance模块**
- **功能**：维修管理
- **主要模型**：Maintenance
- **主要接口**：
  - GET /api/maintenance/ - 获取维修列表
  - POST /api/maintenance/ - 创建维修记录
  - PUT /api/maintenance/{id}/ - 更新维修记录

**reports模块**
- **功能**：报表统计
- **主要接口**：
  - GET /api/reports/borrow-stats/ - 借用统计
  - GET /api/reports/stock-stats/ - 库存统计
  - GET /api/reports/maintenance-stats/ - 维修统计

#### 5.2.3 认证与授权

**JWT认证流程**：
1. 用户登录，提交用户名和密码
2. 后端验证成功，生成Access Token（30分钟有效期）和Refresh Token（1天有效期）
3. 前端存储Token，后续请求在Header中携带Token
4. 后端验证Token，提取用户信息
5. Token过期后，使用Refresh Token刷新Access Token

**权限控制**：
- **基于角色的访问控制（RBAC）**：通过UserRole关联表实现
- **Django权限系统**：使用Django内置权限系统
- **自定义权限**：在permissions.py中定义自定义权限类

### 5.3 前端模块设计

#### 5.3.1 项目结构
```
frontend/
├── src/
│   ├── api/           # API接口封装
│   ├── assets/        # 静态资源
│   ├── components/    # 公共组件
│   ├── config/        # 配置文件
│   ├── constants/     # 常量定义
│   ├── directives/    # 自定义指令
│   ├── layouts/       # 布局组件
│   ├── router/        # 路由配置
│   ├── stores/        # Pinia状态管理
│   ├── types/         # TypeScript类型定义
│   ├── utils/         # 工具函数
│   ├── views/         # 页面组件
│   ├── App.vue        # 根组件
│   └── main.ts        # 入口文件
├── index.html         # HTML模板
├── package.json       # 依赖配置
└── vite.config.ts     # Vite配置
```

#### 5.3.2 核心页面

**用户管理页面**
- 用户列表：展示所有用户，支持搜索、分页
- 用户详情：查看用户详细信息
- 用户编辑：编辑用户信息
- 角色分配：为用户分配角色

**设备管理页面**
- 设备列表：展示所有设备，支持按分类、状态筛选
- 设备详情：查看设备详细信息
- 设备添加：添加新设备
- 设备编辑：编辑设备信息
- 设备状态：管理设备状态

**借用管理页面**
- 申请列表：展示所有借用申请
- 申请详情：查看申请详细信息
- 申请审批：审批借用申请
- 设备出借：设备出借操作
- 设备归还：设备归还验收

**耗材管理页面**
- 耗材列表：展示所有耗材
- 耗材详情：查看耗材详细信息
- 库存管理：管理耗材库存
- 出入库记录：查看库存交易记录

**维修管理页面**
- 维修列表：展示所有维修记录
- 维修详情：查看维修详细信息
- 维修处理：处理维修请求

**报表统计页面**
- 借用统计：展示借用数据统计
- 库存统计：展示库存数据统计
- 维修统计：展示维修数据统计

#### 5.3.3 状态管理

使用Pinia进行状态管理，主要Store：
- **userStore**：用户信息、登录状态
- **equipmentStore**：设备列表、筛选条件
- **borrowStore**：借用申请列表、审批状态
- **consumableStore**：耗材列表、库存信息

### 5.4 API接口设计

#### 5.4.1 接口规范
- **RESTful风格**：遵循RESTful API设计规范
- **统一响应格式**：
  ```json
  {
    "code": 200,
    "message": "success",
    "data": {}
  }
  ```
- **认证方式**：JWT Token认证
- **分页参数**：page、page_size
- **错误处理**：统一的错误码和错误信息

#### 5.4.2 接口清单

**认证接口**
- POST /api/auth/login/ - 用户登录
- POST /api/auth/logout/ - 用户登出
- POST /api/auth/refresh/ - 刷新Token

**用户接口**
- GET /api/users/ - 获取用户列表
- POST /api/users/ - 创建用户
- GET /api/users/{id}/ - 获取用户详情
- PUT /api/users/{id}/ - 更新用户
- DELETE /api/users/{id}/ - 删除用户

**角色接口**
- GET /api/roles/ - 获取角色列表
- POST /api/roles/ - 创建角色
- GET /api/roles/{id}/ - 获取角色详情
- PUT /api/roles/{id}/ - 更新角色
- DELETE /api/roles/{id}/ - 删除角色

**设备接口**
- GET /api/equipment/ - 获取设备列表
- POST /api/equipment/ - 创建设备
- GET /api/equipment/{id}/ - 获取设备详情
- PUT /api/equipment/{id}/ - 更新设备
- DELETE /api/equipment/{id}/ - 删除设备

**耗材接口**
- GET /api/consumables/ - 获取耗材列表
- POST /api/consumables/ - 创建耗材
- GET /api/consumables/{id}/ - 获取耗材详情
- PUT /api/consumables/{id}/ - 更新耗材
- DELETE /api/consumables/{id}/ - 删除耗材

**借用申请接口**
- GET /api/borrow-requests/ - 获取借用申请列表
- POST /api/borrow-requests/ - 创建借用申请
- GET /api/borrow-requests/{id}/ - 获取申请详情
- POST /api/borrow-requests/{id}/approve/ - 审批申请
- POST /api/borrow-requests/{id}/out/ - 设备出借
- POST /api/borrow-requests/{id}/return/ - 设备归还

**维修接口**
- GET /api/maintenance/ - 获取维修列表
- POST /api/maintenance/ - 创建维修记录
- GET /api/maintenance/{id}/ - 获取维修详情
- PUT /api/maintenance/{id}/ - 更新维修记录
- DELETE /api/maintenance/{id}/ - 删除维修记录

**报表接口**
- GET /api/reports/borrow-stats/ - 借用统计
- GET /api/reports/stock-stats/ - 库存统计
- GET /api/reports/maintenance-stats/ - 维修统计

### 5.5 部署与运维

#### 5.5.1 环境配置
- **开发环境**：使用.env文件配置环境变量
- **生产环境**：使用环境变量或配置中心
- **数据库配置**：MySQL连接配置
- **日志配置**：Django日志系统

#### 5.5.2 部署方案
- **后端部署**：使用Gunicorn + Nginx
- **前端部署**：使用Vite构建，Nginx托管静态文件
- **数据库部署**：MySQL主从复制或集群
- **缓存部署**：Redis缓存热点数据

#### 5.5.3 监控与维护
- **日志监控**：收集和分析系统日志
- **性能监控**：监控系统性能指标
- **错误监控**：捕获和跟踪错误
- **数据备份**：定期备份数据库

---

## 6. 数据库范式分析

### 6.1 范式评估

#### 6.1.1 第一范式（1NF）
✅ **完全符合**

**定义**：每个字段都是不可再分的最小数据单位，表中的每一列都是原子的。

**验证**：
- 所有13张表的字段都是原子的，没有复合字段
- 没有重复组或多值属性
- 每个字段都有明确的数据类型和约束

#### 6.1.2 第二范式（2NF）
✅ **完全符合**

**定义**：在满足1NF的基础上，非主键字段完全依赖于主键，不存在部分依赖。

**验证**：
- 所有表都有明确的主键（id字段）
- 所有非主键字段都完全依赖于主键
- 没有复合主键，因此不存在部分依赖问题

#### 6.1.3 第三范式（3NF）
✅ **95%符合**

**定义**：在满足2NF的基础上，非主键字段不传递依赖于主键。

**验证**：
- 大部分表完全符合3NF
- **例外情况**：`borrow_request`表包含`approver`、`approved_at`、`rejected_at`字段
  - 这些字段可以通过`borrow_approval`表获取
  - 存在轻微的传递依赖：`borrow_request.id → borrow_approval.approver → borrow_request.approver`
  - **设计理由**：这是有意的设计，目的是提高查询效率和简化业务逻辑

### 6.2 设计亮点

#### 6.2.1 外键设计
- **CASCADE删除**：用于强关联关系，如UserRole表
- **PROTECT删除**：用于保护重要数据，如Equipment表
- **SET_NULL删除**：用于可选关联，如StockTxn表的related_request字段

#### 6.2.2 索引优化
- **复合索引**：针对高频查询组合，如BorrowRequest表的`(applicant, status, type)`索引
- **单列索引**：针对状态字段，如Equipment表的status索引
- **唯一索引**：确保数据唯一性，如UserRole表的唯一约束

#### 6.2.3 约束完善
- **唯一约束**：确保关键字段的唯一性
- **非空约束**：关键字段设置非空约束
- **外键约束**：保证数据引用完整性

#### 6.2.4 时间戳统一
- 所有表包含`created_at`和`updated_at`字段
- 统一的时间戳管理，便于追踪数据变更

### 6.3 优化建议

#### 6.3.1 数据一致性
- **触发器**：可以使用数据库触发器保持`borrow_request`与`borrow_approval`表的一致性
- **应用层逻辑**：在应用层实现事务，确保数据一致性

#### 6.3.2 性能优化
- **添加索引**：为高频查询字段添加更多索引
- **查询优化**：使用EXPLAIN分析慢查询，优化SQL语句
- **缓存策略**：对热点数据使用缓存

#### 6.3.3 扩展性
- **分区表**：对大表进行分区，提高查询性能
- **读写分离**：实现数据库读写分离，提高并发能力
- **分库分表**：根据业务增长情况，考虑分库分表

---

## 7. 数据库引擎配置

### 7.1 数据库类型

**MySQL数据库**

从`backend/project/settings.py`配置可以看到：
```python
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": os.getenv("MYSQL_DATABASE", "lab_manage"),
        "USER": os.getenv("MYSQL_USER", "root"),
        "PASSWORD": os.getenv("MYSQL_PASSWORD", ""),
        "HOST": os.getenv("MYSQL_HOST", "127.0.0.1"),
        "PORT": os.getenv("MYSQL_PORT", "3306"),
        "OPTIONS": {"charset": "utf8mb4"},
    }
}
```

### 7.2 存储引擎

**InnoDB引擎**


#### 7.2.1 为什么使用InnoDB？

1. **MySQL默认引擎**
   - 从MySQL 5.5.5版本开始，InnoDB已成为MySQL的默认存储引擎
   - Django创建表时，如果没有显式指定引擎，会使用MySQL服务器的默认引擎

2. **外键约束要求**
   - 项目使用了大量外键关系，必须使用InnoDB引擎
   - MyISAM引擎不支持外键约束

3. **事务支持**
   - 业务逻辑涉及复杂的借用流程和库存管理
   - 需要事务支持来保证数据一致性
   - InnoDB支持ACID事务特性

4. **行级锁**
   - InnoDB支持行级锁，提高并发性能
   - MyISAM只支持表级锁，并发性能较差

5. **崩溃恢复**
   - InnoDB有完善的崩溃恢复机制
   - 保证数据不会因为系统崩溃而丢失

#### 7.2.2 InnoDB特性

**事务支持（ACID）**
- **原子性（Atomicity）**：事务中的操作要么全部成功，要么全部失败
- **一致性（Consistency）**：事务执行前后，数据库从一个一致性状态转换到另一个一致性状态
- **隔离性（Isolation）**：并发执行的事务之间互不干扰
- **持久性（Durability）**：事务提交后，对数据库的修改是永久的

**外键约束**
- 确保数据引用完整性
- 防止孤儿记录
- 支持级联操作（CASCADE、SET NULL等）

**行级锁**
- 提高并发性能
- 减少锁冲突
- 支持高并发场景

**崩溃恢复**
- 使用redo log和undo log
- 保证数据不丢失
- 快速恢复

### 7.3 字符集配置

**utf8mb4字符集**

```python
"OPTIONS": {"charset": "utf8mb4"}
```

**为什么使用utf8mb4？**

1. **完整Unicode支持**
   - 支持所有Unicode字符，包括emoji
   - utf8只能存储3字节的字符，utf8mb4可以存储4字节的字符

2. **向后兼容**
   - utf8mb4是utf8的超集
   - 完全兼容utf8编码

3. **国际化支持**
   - 支持多语言
   - 适合国际化应用



### 7.4 数据库配置总结

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 数据库类型 | MySQL | 关系型数据库 |
| 存储引擎 | InnoDB | 默认引擎，支持事务和外键 |
| 字符集 | utf8mb4 | 支持完整Unicode |
| 排序规则 | utf8mb4_unicode_ci | 不区分大小写排序 |
| 连接方式 | TCP/IP | 网络连接 |
| 默认端口 | 3306 | MySQL默认端口 |

---

## 8. 总结

本项目是一个完整的实验室设备与耗材借用管理系统，具有以下特点：

### 8.1 技术特点
- **前后端分离**：Vue3 + Django REST Framework
- **RESTful API**：标准化的API接口设计
- **JWT认证**：安全的用户认证机制
- **数据库设计**：规范的数据库设计，符合3NF
- **权限控制**：基于角色的访问控制（RBAC）

### 8.2 业务特点
- **完整流程**：申请-审批-出借-归还全流程管理
- **状态管理**：完善的状态机设计
- **库存管理**：耗材库存监控和预警
- **维修管理**：设备维修全流程跟踪
- **数据统计**：丰富的报表统计功能

### 8.3 数据库特点
- **13张业务表**：覆盖所有业务场景
- **外键约束**：保证数据完整性
- **索引优化**：提高查询性能
- **时间戳统一**：便于追踪数据变更
- **InnoDB引擎**：支持事务和外键

### 8.4 扩展性
- **模块化设计**：前后端模块化，便于扩展
- **灵活的状态机**：易于添加新的状态
- **可扩展的角色系统**：支持添加新角色
- **丰富的API**：便于集成其他系统

---

## 附录

### A. 项目文件结构
```
final_homework/
├── backend/              # 后端项目
│   ├── accounts/         # 用户与角色模块
│   ├── borrowing/        # 借用管理模块
│   ├── inventory/        # 设备与库存模块
│   ├── maintenance/      # 维修管理模块
│   ├── reports/          # 报表统计模块
│   └── project/          # 项目配置
├── frontend/             # 前端项目
│   ├── src/
│   │   ├── api/          # API接口
│   │   ├── components/   # 公共组件
│   │   ├── views/        # 页面组件
│   │   └── router/       # 路由配置
│   └── package.json
├── docs/                 # 项目文档
│   ├── SPEC.md          # 功能规范
│   ├── ENV.md           # 环境配置
│   └── STATUS.md        # 开发状态
├── README.md            # 项目说明
└── 项目说明文档.md       # 本文档
```

### B. 快速开始

#### B.1 后端启动
```bash
cd backend
pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

#### B.2 前端启动
```bash
cd frontend
npm install
npm run dev
```

#### B.3 数据库初始化
```bash
# 创建数据库
mysql -u root -p
CREATE DATABASE lab_manage CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 运行迁移
python manage.py migrate

# 创建超级用户
python manage.py createsuperuser
```

### C. 常见问题

**Q: 如何添加新的角色？**
A: 在数据库的role表中添加新记录，或在Django Admin中创建。

**Q: 如何修改设备状态？**
A: 通过API接口修改equipment表的status字段，系统会自动记录状态变更日志。

**Q: 如何备份和恢复数据库？**
A: 使用mysqldump备份，使用mysql命令恢复。

**Q: 如何查看系统日志？**
A: 日志文件位于backend/logs/目录（如果配置了日志）。

---

